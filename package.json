{
  "name": "db-sentinel-agent-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:config": "node scripts/sync-config.mjs",
    "predev": "npm run sync:config",
    "dev": "vite",
    "prebuild": "npm run sync:config",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "qa:browser": "node scripts/qa-browser.mjs",
    "qa:docs:images": "node scripts/qa-doc-images.mjs",
    "qa:docs:links": "node scripts/qa-doc-links.mjs"
  },
  "dependencies": {
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "recharts": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "playwright": "^1.61.0",
    "typescript": "latest",
    "vite": "latest"
  }
}
